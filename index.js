const testVar = {}

function testFunc() {
  return "hi"
}

function superbowlWin = Array => {
  let winner = Array.find( game => game.result === "W")
  if(winner) = {
    return winner.year
  } else
  return undefined
}
